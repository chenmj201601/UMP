CREATE FUNCTION F_00_003 (AStrNumber VARCHAR2) --F_BASIC_ISNUMERIC
   RETURN NUMBER
AS --是否是数字判断
   LNumberConvert   NUMBER;
BEGIN
   LNumberConvert := TO_NUMBER(AStrNumber);
   RETURN 1;
EXCEPTION
   WHEN OTHERS
   THEN
      RETURN 0;
END;
