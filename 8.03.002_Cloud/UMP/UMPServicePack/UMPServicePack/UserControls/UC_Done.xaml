<UserControl x:Class="UMPServicePack.UserControls.UC_Done"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border Style="{DynamicResource BorderInChildeWindowDetail}" VerticalAlignment="Stretch" Margin="0,0,0,0">
            <Grid Name="grdResult" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <Label Name="lblResult" Style="{DynamicResource LabelInTitle}" Content="{DynamicResource string49}"></Label>
                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Margin="20,80,10,70">
                    <StackPanel Name="spResult" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0"></StackPanel>
                </ScrollViewer>
                <Button VerticalAlignment="Bottom" Name="btnExit" Content="{DynamicResource string6}" HorizontalAlignment="Right" Margin="0,10,25,30"></Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>

    