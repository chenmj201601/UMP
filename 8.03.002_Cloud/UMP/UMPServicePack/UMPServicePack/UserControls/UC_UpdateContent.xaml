<UserControl x:Class="UMPServicePack.UserControls.UC_UpdateContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
    <Grid>
        <Border Style="{DynamicResource BorderInChildeWindowDetail}" VerticalAlignment="Stretch" Margin="0" >
            <Grid>
                <Border Style="{DynamicResource BorderInChildeWindowDetail}" VerticalAlignment="Stretch" Margin="0,0,0,70" Background="Transparent" >
                    <Grid>
                        <Label Content="{DynamicResource string23}" Style="{DynamicResource LabelInTitle}"></Label>
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Margin="20,90,20,0">
                            <StackPanel Name="splContent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">

                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                   
                </Border>
                <Button Name="btnUpgrade" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="100" Content="{DynamicResource string20}" Margin="0,10,150,30"></Button>
                <Button Name="btnTermination" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="100" Content="{DynamicResource string21}" Margin="0,10,25,30"></Button>
            </Grid>

        </Border>

    </Grid>
</UserControl>
