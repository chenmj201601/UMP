<UserControl x:Class="VoiceCyber.UMP.ScoreSheets.Controls.Design.ControlItemViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Loaded="ControlItemViewer_OnLoaded">
   <Border Name="BorderPanel">
       <Grid>
           <Grid.RowDefinitions>
               <RowDefinition Height="Auto"></RowDefinition>
               <RowDefinition Height="Auto"></RowDefinition>
           </Grid.RowDefinitions>
           <Border Name="BorderTitle" Grid.Row="0">
               <Label Name="LbTitle" HorizontalAlignment="Center" VerticalAlignment="Center" Content="{Binding Title}"></Label>
           </Border>
           <Border Name="BorderContent" Grid.Row="1">
               <Label Name="LbContent" HorizontalAlignment="Left" VerticalAlignment="Center"></Label>
           </Border>
       </Grid>
   </Border>
</UserControl>
