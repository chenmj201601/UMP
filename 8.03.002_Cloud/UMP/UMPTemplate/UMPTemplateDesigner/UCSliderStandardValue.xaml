<UserControl x:Class="UMPTemplateDesigner.UCSliderStandardValue"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Loaded="UCSliderStandardValue_OnLoaded">
    <Grid>
            <Border Height="28">
                <Slider Maximum="{Binding MaxValue}" Minimum="{Binding MinValue}" Interval="{Binding Interval}" Value="{Binding Score}" Margin="3,2" HorizontalAlignment="Stretch" VerticalAlignment="Center"></Slider>
            </Border>
    </Grid>
</UserControl>
