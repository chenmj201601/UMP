<UserControl x:Class="UMPTemplateDesigner.UCTScoreGroupViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Loaded="UCTScoreGroupViewer_OnLoaded">
    <Border Name="BorderPanel">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="150"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Border Name="BorderTitle" Grid.Row="0" Grid.Column="0">
                <Label Name="LbTitle" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="3,1" Content="{Binding Title}"></Label>
            </Border>
            <Border Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2">
                <Grid Name="GridItems" Margin="50,0,0,0">

                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>
